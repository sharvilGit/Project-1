// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCHtelj0bVDt33KXB8hscb9D1bDBZuVh2k",
    authDomain: "sbook-fcaff.firebaseapp.com",
    projectId: "sbook-fcaff",
    storageBucket: "sbook-fcaff.appspot.com",
    messagingSenderId: "500941719966",
    // appId: "1:500941719966:web:70d1f30653c78cf8ee6e8f",
    // measurementId: "G-5GM6CR6GY5"
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
